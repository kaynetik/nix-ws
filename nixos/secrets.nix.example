# Secrets Configuration Template
# Copy this file to secrets.nix and fill in your actual values
# secrets.nix is gitignored and should never be committed

{
  # RKE2 cluster token (required)
  # Generate a new token with: openssl rand -hex 32
  rke2Token = "YOUR_RKE2_TOKEN_HERE";

  # RKE2 server IP address (optional, defaults to 192.168.2.65 if not set)
  # rke2IP = "192.168.2.65";
}
